name: Setup and Install (Ruby on Rails)
description: Set up Ruby and Node.js environments and install dependencies for a Rails application.
inputs:
  install_ruby:
    description: Set to false to skip Ruby setup and installation steps.
    required: false
    default: "true"
  install_node:
    description: Set to false to skip Nodejs setup and installation steps.
    required: false
    default: "true"
runs:
  using: composite
  steps:
    - name: Install packages
      # Skip on self-hosted: our runner image bakes in chrome/chromium, libvips,
      # libjemalloc2, postgresql-client, libpq-dev. Self-hosted containers also
      # run without sudo + with --security-opt no-new-privileges, so this step
      # would fail anyway.
      if: ${{ runner.environment != 'self-hosted' }}
      shell: bash
      run: sudo apt-get update && sudo apt-get install --no-install-recommends -y google-chrome-stable curl libjemalloc2 libvips postgresql-client libpq-dev

    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      if: ${{ inputs.install_ruby == 'true' }}
      with:
        ruby-version: mise.toml
        bundler-cache: true

    - name: Install pnpm
      uses: pnpm/action-setup@v4
      if: ${{ inputs.install_node == 'true' }}

    - uses: actions/setup-node@v6
      if: ${{ inputs.install_node == 'true' }}
      with:
        node-version: "20"
        cache: pnpm

    - name: Install modules
      if: ${{ inputs.install_node == 'true' }}
      shell: bash
      run: pnpm install --frozen-lockfile
