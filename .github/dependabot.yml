version: 2
updates:
  # Bumps SHA pins in .github/workflows/*.yml and actions/*/action.yml.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    labels: [dependencies, github-actions]
    groups:
      actions:
        patterns: ['*']
